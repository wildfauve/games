<%= form_for(@game) do |f| %>
   <%= f.label :play_date %>
   <%= f.text_field :play_date %>
   <%= f.select(:game_type, options_for_select(game_types_select, @game.game_type), include_blank: false, :prompt => "Select a Type") %>   
   <%= f.submit 'Submit'  %>
<% end %>